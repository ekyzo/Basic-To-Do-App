# Express Todo App (Backend + Simple Frontend)

Tiny demo app: Express backend with a static frontend that calls the API.

## Run locally

```bash
npm install
npm run dev
# open http://localhost:3000
